# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# Model checkpoints (large binaries; regenerated by training)
*.pt
*.pth
*.bin

# HuggingFace / torch caches
.cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Multitask data (SST-ids / Quora / STS) is downloaded at runtime by
# download_multitask_data.py and is large (quora-train.csv is ~22 MB), so the
# CSV splits are kept out of git. The small bundled SST/CFIMDB .txt splits from
# the official skeleton stay tracked.
data/*.csv
