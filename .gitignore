# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# Model checkpoints (large binaries; regenerated by training)
*.pt
*.pth
*.bin

# HuggingFace / torch caches
.cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Keep the multitask GLUE-style data out of git if downloaded at runtime
# (SST / CFIMDB are small and bundled by the official skeleton, so they stay)
